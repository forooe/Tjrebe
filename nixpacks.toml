[phases.setup]
nixPkgs = ["...", "aria2"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "aria2c --enable-rpc --rpc-listen-all=false --rpc-max-request-size=10M --max-connection-per-server=16 --split=16 --daemon && python3 index.py"
